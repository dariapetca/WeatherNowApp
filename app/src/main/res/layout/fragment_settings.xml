<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="@dimen/general_fragment_padding"
    tools:context=".weather.WeatherFragment">

    <TextView
        style="@style/settings_textview"
        android:text="@string/alegeti_sistemul_de_referinta" />

    <Spinner
        android:id="@+id/reference_system_spinner"
        style="@style/settings_spinner"
        android:entries="@array/reference_system_array">

    </Spinner>
    <!--
    <TextView
        style="@style/settings_textview"
        android:text="@string/alegeti_tema" />

    <Spinner
        android:id="@+id/theme_spinner"
        style="@style/settings_spinner"
        android:nextFocusDown="@id/language_spinner"
        android:nextFocusUp="@id/reference_system_spinner"
        android:entries="@array/app_theme_array">

    </Spinner>

    <TextView
        style="@style/settings_textview"

        android:text="@string/alegeti_limba" />

    <Spinner
        android:id="@+id/language_spinner"
        style="@style/settings_spinner"
        android:nextFocusUp="@id/theme_spinner"
        android:entries="@array/language_array">

    </Spinner>
-->
</LinearLayout>